// Copy this key binding to your user profile for it to work
// https://github.com/Microsoft/vscode/issues/23757
[
    {
        "key": "ctrl+shift+a",
        "command": "workbench.action.tasks.runTask",
        "args": "Serve",
        "when": "workspaceKeybindings.serveTask.enabled"
    }
]